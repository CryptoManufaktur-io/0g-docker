services:
  geth:
    networks:
      - default
      - ext-network

  0gchaind:
    networks:
      - default
      - ext-network

networks:
  ext-network:
    external: true
    name: ${DOCKER_EXT_NETWORK}
